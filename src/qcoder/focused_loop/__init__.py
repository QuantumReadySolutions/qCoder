"""Bounded first Explorer focused-loop vertical: ``local_aer_stabilizer_clifford_memory_binding_v1``.

This package is source-isolated Phase A work. It owns the frozen schemas, canonical
serialization, exact scientific and statistical arithmetic, durable fixture generation,
named-regime method applicability, privacy projections, the inert plan and separate
authority contracts, the sibling execution receipt, and the currentness/reuse
primitives for one bounded local Qiskit/Aer stabilizer workload.

It deliberately does not integrate with the current-loop coordinator, the Current Step
contract, the strict result manifest module, or the public tool inventory. That
reconciliation is Phase B work against the accepted post-WI-0441 head.
"""

from __future__ import annotations
